Thank you for installing telectl!

Your release is named: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

{{- if .Values.telegram.botToken }}
✅ Bot token configured via Helm values
{{- else }}
⚠️  No bot token configured! You must set it:
    kubectl create secret generic {{ include "telectl.fullname" . }} \
      --namespace {{ .Release.Namespace }} \
      --from-literal=TELEGRAM_BOT_TOKEN="YOUR_BOT_TOKEN" \
      --from-literal=ALLOWED_USER_IDS="123456789" \
      --dry-run=client -o yaml | kubectl apply -f -
{{- end }}

{{- if .Values.rbac.create }}
{{- if .Values.rbac.clusterScoped }}
✅ Cluster-scoped RBAC created (ClusterRole + ClusterRoleBinding)
    The bot has access to all namespaces.
{{- else }}
✅ Namespace-scoped RBAC created (Role + RoleBinding)
    The bot has access to namespace: {{ .Release.Namespace }}
{{- end }}
{{- end }}

To get the bot logs:
    kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "telectl.name" . }},app.kubernetes.io/instance={{ .Release.Name }}

To check the bot configuration:
    kubectl exec -n {{ .Release.Namespace }} deployment/{{ include "telectl.fullname" . }} -- telectl config

To verify the bot is running:
    kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "telectl.name" . }},app.kubernetes.io/instance={{ .Release.Name }}
